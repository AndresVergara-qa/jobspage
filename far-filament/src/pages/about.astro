---
import Layout from '../layouts/Layout.astro';
---

<Layout title="About JobRadar | Connecting Talent with Opportunities">
  <!-- Hero -->
  <section class="relative overflow-hidden bg-white dark:bg-background-dark py-20 lg:py-28">
    <div class="section-container relative z-10">
      <div class="max-w-3xl mx-auto text-center">
        <span class="inline-block px-4 py-1.5 rounded-full bg-primary/10 text-primary text-sm font-semibold mb-6">Our Story</span>
        <h1 class="text-4xl lg:text-5xl font-extrabold text-slate-900 dark:text-white leading-tight mb-6">
          About JobRadar
        </h1>
        <p class="text-xl text-slate-600 dark:text-slate-400 leading-relaxed max-w-lg mx-auto">
          Connecting talent with real, verified opportunities. We're on a mission to bring transparency back to the job search.
        </p>
      </div>
    </div>
  </section>

  <!-- Mission -->
  <section class="py-20 bg-background-light dark:bg-background-dark/50">
    <div class="section-container">
      <div class="max-w-3xl mx-auto">
        <h2 class="text-3xl font-bold text-slate-900 dark:text-white mb-6">Our Mission for Transparency</h2>
        <div class="space-y-6 text-slate-600 dark:text-slate-400 text-lg leading-relaxed">
          <p>
            In an era of "ghost jobs" and outdated listings, JobRadar was founded to serve as a beacon of truth for job seekers. We believe that looking for work shouldn't feel like navigating a minefield of misinformation.
          </p>
          <p>
            Every listing on JobRadar goes through a rigorous vetting process to ensure accessibility, honesty, and freshness. We are dedicated to building a platform where talent can focus on their skills, not on verifying whether a job exists.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- How It Works -->
  <section class="py-24 bg-white dark:bg-background-dark">
    <div class="section-container text-center mb-16">
      <h2 class="text-3xl font-bold text-slate-900 dark:text-white mb-4">How It Works</h2>
      <p class="text-slate-600 dark:text-slate-400 max-w-2xl mx-auto">Our four-pillar approach ensures that every connection made on JobRadar is built on quality data and human-centric support.</p>
    </div>
    <div class="section-container">
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
        {[
          { icon: 'dynamic_feed', title: 'Aggregate Listings', desc: 'We pull from 50,000+ top job boards and corporate career pages globally.' },
          { icon: 'verified_user', title: 'Verify Sources', desc: 'Our AI and human moderators verify the authenticity of every job posting.' },
          { icon: 'psychology', title: 'Help Candidates', desc: 'Smart match algorithms help you find roles that actually fit your profile.' },
          { icon: 'business', title: 'Support Employers', desc: 'We streamline the hiring process with direct, high-quality candidate funnels.' },
        ].map((step) => (
          <div class="p-8 rounded-2xl bg-slate-50 dark:bg-slate-900/50 border border-slate-100 dark:border-slate-800 transition-all hover:border-primary/50 group">
            <div class="w-14 h-14 bg-primary/10 rounded-xl flex items-center justify-center mb-6 group-hover:bg-primary group-hover:text-white transition-colors">
              <span class="material-icons text-primary group-hover:text-white">{step.icon}</span>
            </div>
            <h3 class="text-xl font-bold text-slate-900 dark:text-white mb-3">{step.title}</h3>
            <p class="text-slate-600 dark:text-slate-400 text-sm leading-relaxed">{step.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Trust -->
  <section class="py-24 bg-primary/5 border-y border-primary/10">
    <div class="section-container">
      <div class="text-center mb-16">
        <h2 class="text-3xl font-bold text-slate-900 dark:text-white mb-4">Why Thousands Trust Us</h2>
      </div>
      <div class="grid md:grid-cols-3 gap-8">
        {[
          { icon: 'check_circle', color: 'text-green-500', title: 'Verified Sources', desc: 'Directly pulled from verified corporate systems.' },
          { icon: 'security', color: 'text-orange-500', title: 'No Fake Listings', desc: 'Zero tolerance policy for spam and scams.' },
          { icon: 'update', color: 'text-blue-500', title: 'Updated Daily', desc: 'Listings refreshed every 24 hours for relevance.' },
        ].map((item) => (
          <div class="card p-8 flex items-start gap-4">
            <div class={item.color}>
              <span class="material-icons text-3xl">{item.icon}</span>
            </div>
            <div>
              <h4 class="font-bold text-slate-900 dark:text-white text-lg">{item.title}</h4>
              <p class="text-slate-600 dark:text-slate-400 text-sm mt-1">{item.desc}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Values -->
  <section class="py-24 bg-white dark:bg-background-dark">
    <div class="section-container">
      <h2 class="text-3xl font-bold text-slate-900 dark:text-white mb-12">Our Core Values</h2>
      <div class="grid md:grid-cols-2 gap-12">
        {[
          { num: '01', title: 'Transparency First', desc: "We believe in being open about where data comes from and how salaries are calculated. No hidden agendas, just the facts." },
          { num: '02', title: 'Quality over Quantity', desc: 'We would rather list 100 verified positions than 10,000 questionable ones. Every listing earns its place.' },
          { num: '03', title: 'Respect for Candidates', desc: "Your time is valuable. We don't gatekeep information behind paywalls, and we never sell your data." },
          { num: '04', title: 'Continuous Improvement', desc: "We're always iterating on our verification algorithms and user experience based on community feedback." },
        ].map((value) => (
          <div class="flex gap-6">
            <div class="text-primary font-black text-4xl opacity-20">{value.num}</div>
            <div>
              <h3 class="text-xl font-bold text-slate-900 dark:text-white mb-3">{value.title}</h3>
              <p class="text-slate-600 dark:text-slate-400 leading-relaxed">{value.desc}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
</Layout>
